BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Unit] ADD [description] VARCHAR(255);

-- AlterTable
ALTER TABLE [dbo].[UtilityType] ADD [description] VARCHAR(255);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
